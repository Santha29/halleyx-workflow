{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "▶ Start Halleyx Workflow",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/start.py",
      "console": "integratedTerminal",
      "justMyCode": true,
      "cwd": "${workspaceFolder}"
    }
  ]
}
